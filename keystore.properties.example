# Copia este fichero a "keystore.properties" y rellena los valores.
# keystore.properties está en .gitignore: nunca se commitea.

# Ruta al keystore de subida, relativa a la raiz del proyecto o absoluta.
storeFile=upload-keystore.jks
storePassword=
keyAlias=
keyPassword=
